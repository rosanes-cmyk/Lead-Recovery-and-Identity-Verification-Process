# Lead Recovery & Seller Identity Verification Automation — configuration.
# Copy this file to ".env" and adjust as needed. Never put passwords here —
# the operator logs in manually in the browser window; the app stores no
# credentials.

# ---------------------------------------------------------------------------
# Safety flags. These control whether the app is allowed to make real changes.
# The whole point of the first version is that everything stays FALSE / dry-run:
# the app researches and prepares, but a human approves and executes.
# ---------------------------------------------------------------------------
LIVE_MODE=false          # false = never write to any CRM
DRY_RUN=true             # true = prepare only, make no changes
AUTO_OUTREACH=false      # never contact anyone automatically
AUTO_SAVE_NOTE=false     # never save the REI BlackBook note automatically
AUTO_CHANGE_STATUS=false # never change the lead status automatically
AUTO_CREATE_TASK=false   # never create the next task automatically
AUTO_REASSIGN=false      # never reassign the lead automatically

# People search: Cherry Hombre approved TruePeopleSearch. Results are treated as
# clues only and cross-checked with official / property-data sources.
PEOPLE_SEARCH_ENABLED=true

# ---------------------------------------------------------------------------
# Demo mode: explore the whole flow with synthetic data and NO real browser or
# logins. Great for a first look. Set to false to run real investigations.
# ---------------------------------------------------------------------------
DEMO_MODE=false

# ---------------------------------------------------------------------------
# Browser automation
# ---------------------------------------------------------------------------
# HEADLESS=false shows the browser so the operator can log in when prompted.
# Keep this false on the operator's machine.
HEADLESS=false
# Persistent profile directory. Logins persist here between runs so the
# operator does not have to sign in every time. This folder can contain
# session cookies — keep it private and never commit it.
USER_DATA_DIR=.browser-profile
# Optional: use real Google Chrome instead of the bundled Chromium.
# Leave blank to use Playwright's Chromium. Set to "chrome" for installed Chrome.
CHROME_CHANNEL=

# ---------------------------------------------------------------------------
# Investigation time limits (minutes). Enhanced requires approval.
# ---------------------------------------------------------------------------
STANDARD_TIME_LIMIT_MIN=30
ENHANCED_TIME_LIMIT_MIN=60

# ---------------------------------------------------------------------------
# Property Enrichment tab (batch: a CSV of addresses -> PropertyRadar + the web)
# ---------------------------------------------------------------------------
# Pause between rows, in milliseconds (a little random jitter is added). Slower
# is gentler on PropertyRadar; 2500 is roughly 45-60 minutes for ~230 rows.
ENRICH_DELAY_MS=2500
# Also search the web (DuckDuckGo, no login) for listing / county links per row.
ENRICH_WEB_SEARCH=true
# Keep one PropertyRadar result screenshot per row as evidence.
ENRICH_SCREENSHOTS=true
# Also open each address's Zillow page for property type / listing status / county.
ENRICH_ZILLOW=true

# ---------------------------------------------------------------------------
# Server
# ---------------------------------------------------------------------------
PORT=4319

# ---------------------------------------------------------------------------
# Temporary sharing — let someone else open the app for a while.
# ---------------------------------------------------------------------------
# Leave SHARE_PASSWORD blank and the server listens on this machine only
# (127.0.0.1): nothing on the network can reach it. Set a password and the
# server also listens on the LAN, with every page, API call and CSV download
# behind that one password.
#
# Remember what a guest can do once they are in: start runs in YOUR signed-in
# PropertyRadar browser and download every CSV in runs/. Share it only with
# people you would hand the laptop to, and clear the password when you are done.
#SHARE_PASSWORD=pick-a-long-passphrase
# How long a sign-in lasts before the guest must type the password again.
SHARE_TTL_HOURS=12
# Which interface to bind. Normally leave this alone — it is chosen for you
# from SHARE_PASSWORD above. Set it only to override (e.g. 0.0.0.0).
#HOST=0.0.0.0
